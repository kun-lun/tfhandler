#!/bin/bash -exu

root_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

pushd ${root_dir}/../terraform
  go-bindata -pkg terraform -mode 0740 -ignore "terraform.tf*" -o templates.go templates
popd
